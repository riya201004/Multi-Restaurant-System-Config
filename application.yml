logging:
  level:
    root: INFO

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    config:
      profile: default
      fail-fast: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
